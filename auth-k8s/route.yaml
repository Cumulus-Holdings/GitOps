apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: auth-api
  name: auth-api
spec:
  port:
    targetPort: 1880
  to:
    kind: Service
    name: auth-api
    weight: 100
  wildcardPolicy: None
