apiVersion: v1
kind: Service
metadata:
  labels:
    app: auth-api
  name: auth-api
spec:
  ports:
  - name: 1880-tcp
    port: 1880
    protocol: TCP
    targetPort: 1880
  selector:
    app: auth-api
  sessionAffinity: None
  type: ClusterIP
